''' Since we're not using a database to store the blockchain data,
we don't actually need a models.py file. The Block and Blockchain classes
defined in blockchain.py serve as our data model for the DLT,
 and we won't be using Django's ORM to interact with a database. '''
